version: 1
mode: enforce
workspace:
  root: .
  readable:
    - "**"
  writable:
    - src/**
    - tests/**
    - docs/**
    - examples/**
    - package.json
    - pnpm-lock.yaml
    - README.md
    - CHANGELOG.md
  neverRead:
    - .env
    - .env.*
    - .ssh/**
    - .gnupg/**
    - .aws/**
    - "**/*.pem"
    - "**/*.key"
    - "**/*_rsa"
    - "**/id_ed25519"
    - "**/secrets/**"
    - "**/.npmrc"
    - "**/.pypirc"
audit:
  path: .agentgate/audit.jsonl
  redactSecrets: true
approval:
  mode: terminal
rules:
  - id: deny-credential-reads
    effect: deny
    tools:
      - fs.read
      - mcp.tool
      - read_file
    paths:
      - .env
      - .env.*
      - .ssh/**
      - .gnupg/**
      - .aws/**
      - "**/*.pem"
      - "**/*.key"
      - "**/*_rsa"
      - "**/id_ed25519"
      - "**/secrets/**"
      - "**/.npmrc"
      - "**/.pypirc"
    reason: Credential reads are blocked
  - id: ask-medium-shell
    effect: ask
    tools:
      - shell.exec
    commandRisk:
      min: medium
    reason: Medium-risk shell commands require approval for package maintenance
  - id: deny-private-network-fetches
    effect: deny
    tools:
      - http.fetch
      - mcp.tool
      - fetch
    urls:
      denyPrivateNetworks: true
      denyLinkLocal: true
      denyLoopback: true
    reason: Private, loopback, and metadata network fetches are blocked
